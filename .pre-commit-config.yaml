# Pre-commit Configuration for EyesDcar
# Version: 2.2
# Date: 23/04/2026
# Python: 3.10

default_install_hook_types: [pre-commit, commit-msg]
default_stages: [pre-commit]
default_language_version:
  python: python3.10

repos:
  # ═══════════════════════════════════════════════════════════
  # PHASE 1 : FORMATTING (Automatic formatting)
  # ═══════════════════════════════════════════════════════════
  - repo: local
    hooks:
      - id: check-commit-message
        name: Check Commit Message Convention
        entry: |
          python3 -c '
          import sys, re
          pattern = r"^\[(FEATURE|FIX|MODEL|REFAC|DOCS|PERF|TEST|SEC|DEVOPS|CHORE)\]\s.*"
          msg = open(sys.argv[1]).read().strip()
          if not re.match(pattern, msg):
              print("\n❌ Invalid commit message format!")
              print("Expected: [TYPE] Description (ex: [FEATURE] Added OBD reader.)\nPossible prefixes: [FEATURE], [FIX], [MODEL], [REFAC], [DOCS], [PERF], [TEST], [SEC], [DEVOPS], [CHORE]")
              sys.exit(1)
          '
        language: system
        stages: [commit-msg]


  - repo: https://github.com/psf/black
    rev: 26.3.1
    hooks:
      - id: black
        name: "Black - Python formatting"
        language_version: python3.10
        args: ['--line-length=88']

  - repo: https://github.com/pycqa/isort
    rev: 8.0.1
    hooks:
      - id: isort
        name: "isort - Import sorting"
        args: ['--profile', 'black', '--line-length', '88']

  - repo: https://github.com/asottile/pyupgrade
    rev: v3.21.2
    hooks:
      - id: pyupgrade
        name: "pyupgrade - Syntax modernization"
        args: ['--py310-plus']

  # ═══════════════════════════════════════════════════════════
  # PHASE 2 : LINTING (Fast static analysis)
  # ═══════════════════════════════════════════════════════════

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.11
    hooks:
      - id: ruff
        name: "Ruff - Fast linting"
        args:
          - --fix
          - --exit-non-zero-on-fix
        types_or: [python, pyi]

  # ═══════════════════════════════════════════════════════════
  # PHASE 3 : SECURITY (Threat detection)
  # ═══════════════════════════════════════════════════════════

  - repo: https://github.com/PyCQA/bandit
    rev: 1.9.4
    hooks:
      - id: bandit
        name: "Bandit - Security scan"
        args:
          - '-c'
          - '.pre-commit/.bandit'
          - '-r'
        exclude: '^tests/|^\.github/scripts/'

  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        name: "detect-secrets - Credentials scan"
        args:
          - '--baseline'
          - '.pre-commit/.secrets.baseline'
        exclude: 'package-lock.json|poetry.lock|.*\.ipynb$'

  # ═══════════════════════════════════════════════════════════
  # PHASE 4 : NOTEBOOKS (Jupyter cleanup)
  # ═══════════════════════════════════════════════════════════

  - repo: https://github.com/kynan/nbstripout
    rev: 0.9.1
    hooks:
      - id: nbstripout
        name: "nbstripout - Notebook cleanup"
        args:
          - '--extra-keys'
          - 'metadata.kernelspec metadata.language_info'

  # ═══════════════════════════════════════════════════════════
  # PHASE 5 : GUARDIANS (Basic checks)
  # ═══════════════════════════════════════════════════════════

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        name: "Remove unnecessary spaces"
        args: ['--markdown-linebreak-ext=md']

      - id: end-of-file-fixer
        name: "Fix end of file"

      - id: check-yaml
        name: "YAML validation"
        args: ['--unsafe']

      - id: check-json
        name: "JSON validation"

      - id: check-merge-conflict
        name: "Merge conflict detection"

      - id: check-added-large-files
        name: "File size verification"
        args: ['--maxkb=500']
        exclude: '\.pt$|\.pth$|\.onnx$'

      - id: detect-private-key
        name: "Private key detection"
